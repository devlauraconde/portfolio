/* PRESETS */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

@font-face {
    font-family: 'Monument Extended'; /* Nome correto da fonte */
    src: url('fonts/MonumentExtended-FreeForPersonalUse/MonumentExtended-Ultrabold.otf') format('opentype');
    font-weight: 800; /* Opcional: 800 é ultrabold */
    font-style: normal;
}


/* VARIABLES */
 :root {
    --body-bg: rgb(237, 237, 236);
    --header-text: white;
    --body-text: #000000;
    --headings: rgb(192, 96, 97);
    --primary: rgb(192, 96, 97);
    --secondary: rgb(151, 76, 77);
    --fill-btn: rgb(202, 187, 187);
    
    font-family: "Inter", sans-serif;
    text-align: center;
    scroll-behavior: smooth;
 }   



#closeIcon,
/* .mobile-nav, */
.desktop-nav {
    display: none; /* Quando aplicas display: none a um elemento, ele desaparece da visualização, mas também não ocupa espaço no layout da página, ou seja, ele é completamente removido do fluxo de layout. */
}

/* BASE STYLES */

body {
    background-color: var(--body-bg); /* cor de fundo do body */
}

main { 
    /* tenho de fazer isto no projeto da debora */
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 3rem 2rem; /* distancia vertical do topo, e distancia horizontal dos lados */
}

section {
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: 470px;
    margin-bottom: 3.5rem;
}

section:last-of-type {
    margin-bottom: 0;
}

h1 {
    font-size: 3rem;
    line-height: 1;
}

h3 {
    margin-bottom: 0.5rem;
    font-size: 1rem;
}

li {
    list-style: none;
}

a {
    text-decoration: none;
    color: inherit; /* inherit; faz com que o elemento herde a cor do seu elemento pai */
}

/* DRY */

main,
footer {
    margin-inline: 1rem; /* margem dos lados do ecra para todo o main */
}

h1,
h2 {
    margin-bottom: 1rem;
}

h1,
h2,
h3 {
    font-weight: 400;
}

.hero-container p:last-of-type,
.about-container p:last-of-type {
    /* Seleciona o último elemento <p> dentro do seu contêiner (que é o .hero-container). A pseudoclasse :last-of-type garante que apenas o último parágrafo será afetado, não importando se existem outros tipos de elementos entre os parágrafos. */
    margin-bottom: 1.5rem;
}

/* UTILITY CLASSES */

.btn {
    /* botão do contact me */
    padding: 0.5rem 1rem; /* largura e comprimento do tamanho do botao */
    border-radius: 5px;
    color: var(--header-text);
    background-color: var(--headings);
    box-shadow: 0 2px var(--secondary);
    transition: background-color 0.3s ease;
}

.btn:hover {
    background-color: var(--secondary);
}

.body-text {
    font-family: "Inter", sans-serif;
    font-size: 0.75rem;
    line-height: 2;
    color: var(--body-text);
}

/* CUSTOM SYTLES */

/* HEADER */

header {
    position: relative; /* position: relative; ainda ocupa o mesmo espaço no layout, mas agora ele pode ser deslocado. */
    display: flex; /* ao dar flex, o header encosta-se acima à esquerda */
    justify-content: space-between; /* Aqui separou o icon/menu de navegacao entre o nome e o icon, cada um para o seu lado */
    align-items: center;
    padding: 1rem; /* aqui faz com que o header fique com 1rem de distancia ao limite da folha */
    color: var(--header-text); /* aqui dei a cor das letras do header */
    background-color: var(--headings); /* aqui dei a cor de fundo do header */
}

.menu-btn {
    cursor: pointer; /* ao o rato passar por cima fica com o pointer */
    background: none; /* tirei o fundo do botao/icon do menu de navegacao */
    border: none; /* tirei a borda do botao/icon do menu de navegacao */
    height: 24px; /* tamanho do icon/botao */
    width: 24px;
    fill: var(--header-text); /* dar preenchimento ao botao/icon de branco */
    transition: fill 0.3s ease; /* quando a propriedade fill mudar, essa mudança será feita ao longo de 0,3 segundos, e a transição será suave (ease), começando devagar, acelerando no meio e desacelerando no final. */
}

.menu-btn:hover {
    fill: var(--fill-btn) /* sempre que o usuário faz hover (passa o mouse) sobre o botão de menu (menu-btn), a cor de preenchimento (fill) será alterada. */
}

/* MOBILE NAV */ 
/* coluna de navegacao quando abrimos o icon do header */

.mobile-nav {
    position: absolute;
    z-index: 50;
    top: 69px; /* distancia do topo até ao incio da coluna (que abre com a navegacao do icon no header) */
    right: 0; /* ajustar a largura necessária a partir do canto direito */
    width: 25%; /* largura com que a coluna ocupara o ecra */
    padding-top: 1rem; /* afastamento da primeira palavra about ate ao inicio da coluna */
    font-size: 1.5rem; /* tamanho da letra*/
    background-color: var(--headings); /* cor de fundo da coluna de navegacao aberta */
    transform: translateX(100%); /*  Isso faz com que a barra de navegação comece fora da tela, empurrada completamente à direita. */
    transition: transform 0.3s ease; /*  faz com que a brra deslize para a direita horizontalmente softmente */
}

.mobile-nav.open { /*  faz com que ao .open a barra se mova (abra). Se fechada está a 100%, quando abrimos queremos que fique a 0% */
    transform: translateX(0%)
}

.mobile-nav li {
    margin-bottom: 1rem; /* afastamento entre texto da coluna de navegacao */
}

.mobile-nav a,
.desktop-nav a {
    position: relative;
    z-index: 10;
    transition: color 0.3s ease; /* quando a propriedade fill mudar, essa mudança será feita ao longo de 0,3 segundos, e a transição será suave (ease), começando devagar, acelerando no meio e desacelerando no final. */
}

.mobile-nav a:hover,
.desktop-nav a:hover {
    color: var(--fill-btn); /* sempre que o usuário faz hover (passa o mouse) sobre o botão da palavra, a cor de preenchimento (fill) será alterada. */
}

/* HERO SECTION */

.hero-img-container {
    position: fixed;
    bottom: 0;
    right: 0;
    background: none;
    width: auto; /* 50% da largura da viewport */
    height: auto; /* para manter proporcao */
    pointer-events: none; /* faz com que o elemento não receba eventos de ponteiro */
}

.hero-img-container img {
    /* imagem de Laura Conde fixa no canto inferior direito */
    position: relative;
    width: 50vw; /* 50% da largura da viewport */
    max-width: 200px;
    height: auto; /* para manter proporcao */
    background-color: none;
}

.connect {
    color: var(--body-text);
}

.hero-text-container {
    position: relative;
    font-family: 'Monument Extended', sans-serif;
    text-align: left;
    display: block;
}

.hero-container p {
    font-size: 1.2rem;
    color: var(--body-text);
}

/* ABOUT SECTION */

.about-container p {
    margin-bottom: 1rem;
}

/* PROJECTS SECTION */

.project-container {
    margin-bottom: 1.5rem;
}

.project-container:last-of-type {
    margin-bottom: 0;
}

.project-img-container {
    /* imagens dos projetos */
    width: 100%;
    height: auto;
    margin-bottom: 0.5rem;
}

.project-img {
    aspect-ratio: 16/9;
    background-color: var(--headings); /* meti um retangulo com cor mas substituimos pela imagem do projeto */
}

.project-techs {
    margin-bottom: 0.5rem;
}

.project-techs span {
    /* linguagens de programacao utilizadas em cada projeto */
    padding: 0.25rem 0.5rem;
    border-radius: 10px;
    font-size: 0.6rem;
    color: var(--header-text);
    background-color: var(--headings);
}

.project-text-container p {
    margin-bottom: 1rem;
    font-size: 0.7rem;
    line-height: 1.5;
}

.project-links a {
    font-size: 0.75rem;
}

/* ART SECTION */

.art-container {
    margin-bottom: 1.5rem;
}

.art-container:last-of-type {
    margin-bottom: 0;
}

.art-img-container {
    /* imagens dos projetos */
    width: 100%;
    height: auto;
    margin-bottom: 0.5rem;
}

.art-img {
    aspect-ratio: 16/9;
    background-color: var(--headings); /* meti um retangulo com cor mas substituimos pela imagem do projeto */
}

.art-text-container p {
    margin-bottom: 1rem;
    font-size: 0.7rem;
    line-height: 1.5;
}

/* CONTACT SECTION */

.socials-container {
    display: flex;
    margin-top: 2rem;
    gap: 1rem;
}

.socials-container svg {
    width: 20px;
    height: 20px;
    left: 0px;
    fill: var(--primary);
    transition: transform 0.3s ease, fill 0.3s ease; /* quando a propriedade fill mudar, essa mudança será feita ao longo de 0,3 segundos, e a transição será suave (ease), começando devagar, acelerando no meio e desacelerando no final. */
}

.socials-container svg:hover {
    /* sempre que o usuário faz hover (passa o mouse) sobre o botão da rede social, a cor de preenchimento (fill) será alterada. */
    transform: scale(1.2);
    fill: var(--secondary);
}

/* FOOTER */

footer {
    position: fixed; /* Fixa o footer no fundo */
    bottom: 0; /* Coloca o footer no fundo da tela */
    left: 50%; /* Centraliza o footer horizontalmente */
    transform: translateX(-90%); /* Ajusta a posição para centralizar */
    font-size: 0.75rem;
    margin-bottom: 0.1rem;
    text-align: center; /* Garante que o texto dentro do footer fique centralizado */
    width: 100%; /* Garantir que o footer ocupe toda a largura da tela */
    padding: 1rem 0; /* Adiciona um pouco de espaçamento ao redor do texto */
    color: var(--body-text); /* Cor do texto */
    z-index: 51;
}

/* MEDIA QUERIES */

@media (min-width: 1024px) { 
    .menu-btn,
    .mobile-nav {
        display: none;
    }

    .desktop-nav {
        display: block;
    }

    .desktop-nav ul{
        display: flex;
        gap: 3rem;
    }

    .hero-container {
        flex-direction: row-reverse;
        justify-content: space-between;
        text-align: left;
    }

    .hero-text-container {
        margin-left: 0; /* Garantir que o texto fique alinhado à esquerda */
        width: 100%; /* Usar 100% da largura para preencher a tela */
        max-width: 600px; /* Limitar a largura máxima a 600px */
    }

    .project-container {
        display: flex;
        gap: 2rem;
        align-items: center;
        text-align: left;
    }

    .project-img-container,
    .project-text-container {
        flex-basis: 50%;
    }

    .socials.container {
        gap: 2rem;
    }

    .socials-container svg {
        width: 35px;
        height: 35px;
    }

    /* FONT SIZES & SPACING */

    header,
    main,
    footer {
        margin-inline: auto;
        padding-inline: 3rem;
    }

    main {
        padding-block: 6rem;
    }

    section {
        width: 100%;
        max-width: 100%;
        margin-bottom: 6.5rem;
    }

    .about-container p {
        margin-bottom: 2rem;
    }

    .project-text-container {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .contact-container p {
        margin-bottom: 1rem;
    }

    footer {
        padding-block: 3rem;
    }

    h1 {
        font-size: 4rem;
    }

    h2 {
        margin-bottom: 2rem;
        font-size: 2.5rem;
    }

    h3,
    .hero-container p {
        font-size: 1.5rem;
    }

    .body-text {
        font-size: 1.25rem;
    }

    header {
        font-size: 1.15rem;
    }

    .project-text-container p,
    .art-text-container p,
    .hero-container p:last-of-type,
    .project-techs span,
    .project-links a,
    .contact-container p,
    footer {
        font-size: 1rem;
    }
}

@media (max-width: 768px) {
    .hero-text-container {
        text-align: center; /* Centraliza o texto para telas pequenas */
        margin-left: 0; /* Remove a margem esquerda */
        margin-right: 0; /* Remove a margem direita */
    }
}

.hero-text-container h1 {
    margin-bottom: 0.5rem;
}

.hero-text-container p {
    margin: 0.25rem 0;
}

.hero-text-container .btn {
    margin-top: 1rem;
}


